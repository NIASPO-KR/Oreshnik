package datacontroller

// static paths
const (
	staticPath       = "/api"
	ItemsPath        = staticPath + "/items"
	PickupPointsPath = staticPath + "/pickupPoints"
	PaymentsPath     = staticPath + "/payments"
)

// users paths
const (
	usersPath = "/api"
	CartPath  = usersPath + "/cart"
)
